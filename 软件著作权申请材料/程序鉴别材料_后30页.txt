# 程序鉴别材料（后30页）

## 文件1: worker.js (后30行)

  };
  
  return templates[type] || templates["保健品"];
}

// 辅助函数（示例）
function getSuggestions(type) {
  switch(type) {
    case "保健品":
      return [
        "增加临床数据引用",
        "替换更具体的痛点"
      ];
    case "OTC药品":
      return [
        "强化医生/药师背书",
        "添加用药禁忌提示"
      ];
    case "中药饮片":
      return [
        "增加传统医学理论支撑",
        "添加适宜人群说明"
      ];
    default:
      return [
        "可增加用户评价",
        "可添加使用场景"
      ];
  }
}